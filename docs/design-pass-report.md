# 디자인 패스 보고서 (2026-09-21)

시각적인 변경만 했다. `src/core/`, 상태/판정 로직, 한글 문구, `data-*`/id 값은 건드리지 않았다.
`npm test` 240개 전부 통과, `npm run build` 로 `dist/index.html` 단일 파일 생성 확인.

## 방향

클레이모피즘의 **구조**(두꺼운 테두리, 큰 반경, 이중 그림자, 통통 튀는 눌림)만 가져오고,
색은 이 프로젝트의 기존 정체성(크림 배경/오렌지 강조/카피바라 갈색)을 그대로 유지했다.
스킬이 제안한 파란색 팔레트와 구글 폰트(Baloo 2 / Comic Neue)는 채택하지 않았다 — 파란색은
카피바라의 세계관과 어긋나고, 웹폰트는 "외부 네트워크 요청 0개" 규칙에 정면으로 위배되며
애초에 한글도 지원하지 않는다. 시스템 폰트 스택을 그대로 썼다.

## 1. 대비 — 결함으로 취급해 가장 먼저 고쳤다

`/tmp` 스크래치 디렉터리에 WCAG 상대휘도 공식으로 대비를 계산하는 스크립트를 짜서, 실제로
쓰이는 전경/배경 쌍을 전부(크림 배경, 흰 카드, 5개 계열 배경 틴트 포함) 계산했다. 눈대중은
하지 않았다.

### Before

| 전경 (용도) | 배경 | 비율 | 판정 |
|---|---|---|---|
| `--muted` `#8a7c6b` | `--bg` `#fdfaf5` | 3.90:1 | 실패 |
| `--muted` | `--card` `#ffffff` | 4.06:1 | 실패 |
| `--muted` | series bg 1 `#eef7e4` | 3.68:1 | 실패 |
| `--muted` | series bg 2 `#e2f2f7` | 3.53:1 | 실패 |
| `--muted` | series bg 3 `#fdf0dd` | 3.61:1 | 실패 |
| `--muted` | series bg 4 `#eaf1fa` | 3.57:1 | 실패 |
| `--muted` | series bg 5 `#ece7fa` | 3.36:1 | 실패 |
| `.result-xp` `#c07a22` | `--bg` | 3.33:1 | 실패 |
| `.result-xp` | series bg 5 (최악) | 2.87:1 | 실패 |
| `--accent` `#f0912f` (텍스트로) | `--bg` | 2.29:1 | 실패 |
| `--accent` (텍스트로) | series bg 5 (최악) | 1.98:1 | 실패 |
| 흰 글자 (`.btn-primary`) | `--accent` `#f0912f` | 2.39:1 | 실패 (명세엔 없었지만 발견) |
| `.pill` 글자 `#7a6a55` | `.pill` 배경 `#f2ede4` | 4.48:1 | 근소하게 실패 |
| `.capy-slot.is-locked` `#c9b9a3` | `#f2ede4` | 1.64:1 | 실패(아이콘성 텍스트) |
| `.explain-note` (`--bad` `#e8564f`) | `--bg` | 3.43:1 | 실패 |

### After

| 전경 (새 토큰) | 배경 | 비율 | 판정 |
|---|---|---|---|
| `--muted` `#5f5445` | `--bg` | 7.10:1 | 통과 |
| `--muted` | `--card` | 7.39:1 | 통과 |
| `--muted` | series bg 1~5 (최악 bg5) | 6.71~6.12:1 | 통과 |
| `--accent-text` `#8a4e10` (`.result-xp`, 태그, 배지, 라벨 등 주황 글자 전부) | `--bg` | 6.35:1 | 통과 |
| `--accent-text` | `--card` | 6.61:1 | 통과 |
| `--accent-text` | series bg 1~5 (최악 bg5) | 6.00~5.47:1 | 통과 |
| 흰 글자 (`.btn-primary`) | `--accent-text` (버튼 배경으로 승격) | 6.61:1 | 통과 |
| `.pill` 글자 → `var(--muted)` | `.pill` 배경 | 6.34:1 | 통과 |
| `.capy-slot.is-locked` → `--locked-fg` `#8f7c66` | `#f2ede4` | 3.44:1 | 통과 (장식적 "?" 아이콘, 비텍스트 3:1 기준 적용) |
| `.explain-note` → `--bad-text` `#c03028` | `--bg` | 6.00:1 | 통과 |

주황을 **표면/테두리**로 쓰는 곳(기본 버튼 면 제외 — 아래 참고, 활성 입력칸 링, 추천 타일
테두리)은 명세대로 밝은 `--accent` `#f0912f` 를 그대로 두었다. 이런 용도는 텍스트가 아니라
3:1 기준이고, 굵은 테두리 + 그림자 + 다른 시각 단서가 함께 있어 색 하나에 의존하지 않는다.

## 2. 토큰 — 글자 크기/여백 스케일

`:root` 에 `--fs-12/14/16/20/24/32`, `--sp-4/8/12/16/24/32/48` 을 추가하고 스타일시트
전체에 적용했다. 본문은 16px/line-height 1.5. 아이가 읽는 글자 중 14px 미만이던 것들
(`.quiz-title` 13→14, `.tile em` 11→14, `.pill` 12→14, `.badge` 12→14, `.capy-slot`
숫자 라벨 10→14, `.result-xp`/`.result-badges`/`.records-table`/`.muted` 13→14 등)을
전부 14 이상으로 올렸다. 세로셈 grid 안(캐리 15px, 셀 24px)은 이미 14 이상이라 그대로 뒀다.

색 토큰도 필요한 만큼 추가했다: `--accent-text`, `--bad-text`, `--white`,
`--accent-rgb`/`--ink-rgb`(그림자·링을 `rgba()`로 합성하기 위해), `--track-bg`류,
`--pill-bg`, `--badge-*`, `--locked-*`, `--keypad-active-bg`. 스타일시트 안에 `:root`
밖에 남은 원시 hex는 없다(SVG를 그리는 JS 안의 색은 `capybara.js`가 원래부터 쓰던 방식과
동일해서 손대지 않았다).

## 3. 터치 타깃

44×44px 미만이던 두 곳을 고쳤다: `.pill-button`(기록실 보기)과 `.quiz-exit`(집으로).
둘 다 시각적 크기는 그대로 두고 `::after` 로 투명한 히트 영역만 44px 이상으로 넓혔다
(`position:absolute; inset:50% auto auto 50%; width/height:max(44px,100%)`). 다른 버튼
(`.btn-primary`, `.btn-ghost`, `.tile`, `.keypad button`)은 계산해보니 이미 44px를 넘었다.

## 4. 반응형

미디어 쿼리가 하나도 없던 상태에서 모바일 우선으로 다시 짰다. 375 / 768 / 1024px 를
Playwright로 직접 렌더링해 확인했다.

- 카테고리 타일: 기본(좁은 폰) 1열, `min-width:480px` 부터 2열.
- 문제 화면: `.quiz-board` 래퍼를 새로 두고(마크업 최소 변경) 기본은 세로로 쌓고,
  `min-width:768px` 부터 grid와 keypad를 가로로 나란히 배치.
- 세로셈 grid: `overflow-x:auto` 안전망을 달았지만 실제로는 어느 폭에서도 넘치지 않는다
  (최대 5자리 × 44px+gap ≈ 240px). 375px에서 스크린샷으로 정렬 확인, 768px에서도 그대로
  유지됨을 확인했다.
- 기록실 표: `.records-table-scroll` 래퍼(마크업 최소 변경)로 감싸 혹시 모를 좁은 화면에서
  가로 스크롤만 표에 국한되게 했다.

## 5. 포커스 · 모션 · 아이콘

- 모든 버튼/링크/입력에 `:focus-visible` 링 (3px, offset 2px, `--accent-text` 색)을 추가했다.
- `prefers-reduced-motion: reduce` 에서 모든 애니메이션/트랜지션을 사실상 끄고, 레벨업
  팝(`capy-pop`)만 특별 처리했다 — 애니메이션 대신 카피바라 주위에 고정된 링(`box-shadow`)을
  둘러 "상태가 바뀌었다"는 게 계속 보이게 했다.
- 숫자판의 `←`/`✓` 를 인라인 SVG(백스페이스 화살표 / 체크)로 바꿨다. **`data-key` 값은
  `"←"`/`"✓"` 그대로 유지**했고(테스트·키보드 핸들러가 이 값을 그대로 씀), 버튼 안에 그려지는
  내용과 `aria-label`("지우기"/"확인")만 바꿨다.
- 연속 출석 옆 🔥 를 인라인 SVG 불꽃 아이콘으로 바꿨다(집 화면, 기록실 둘 다). ✨(레벨업
  반짝임)는 명세대로 손대지 않았다.

## 6. 세로셈 grid

입력칸(`.g-input`, `.g-carry`)은 클레이 처리를 하지 않고 테두리(2px)·모서리(8→10px)만
아주 살짝만 키웠다. 버튼/타일/카드/히어로에만 두꺼운 테두리(3px)·큰 반경(16~22px)·이중
그림자(인셋 하이라이트 + 아웃셋 그림자)·바운스 눌림(`cubic-bezier(0.34,1.56,0.64,1)`,
180ms)을 적용했다. 캐리 박스가 자리 위에 오는 정렬, 두 번째 부분곱의 1의 자리가 비는 것,
칸 격자 정렬은 `verticalGrid.js`(로직)와 `.g-row`/`.g-cell` 관련 CSS를 건드리지 않아서
그대로다 — 375px/768px 스크린샷으로 직접 확인했다.

## 명세에 없었지만 내가 판단해서 처리한 것

- **`.btn-primary`(한 판 더 버튼)의 흰 글자 대 주황 배경 대비가 2.39:1로 심각하게
  실패**하고 있었다(명세가 지목한 세 개 목록에는 없었음). 버튼 배경을 밝은 `--accent`
  대신 새 `--accent-text`(#8a4e10)로 바꿔 6.61:1을 확보했다 — "주황을 표면으로 쓰되 텍스트가
  얹히는 표면은 그 텍스트가 읽혀야 한다"는 원칙을 지키려는 선택이다. 대신 활성 입력 링·추천
  타일 테두리처럼 텍스트가 얹히지 않는 곳은 밝은 `--accent` 그대로 뒀다.
- `.pill` 글자(`#7a6a55`, 4.48:1로 근소하게 미달)와 `.explain-note`(`--bad` 텍스트,
  3.43:1)도 명세엔 없었지만 실측에서 발견해 같이 고쳤다.
- 카피바라에게 아주 은은한 유휴 바운스(3.4초 주기, ±3px)를 새로 추가했다 — 명세가
  "카피바라가 계속 bob 한다"고 전제했는데 실제 코드엔 없어서, 클레이 방향에 맞게 최소한으로
  넣고 `prefers-reduced-motion` 에서 바로 끄게 했다.
- 문제 화면에서 `#message`(정답/오답 피드백 문구) 위치를 grid 아래에서 title 아래·grid 위로
  옮겼다 — `.quiz-board`가 grid+keypad를 가로 배치로 묶으면서 message를 그 안에 넣으면
  레이아웃이 어색해져서, 상태 표시줄처럼 위쪽에 고정했다. 동작·테스트에는 영향 없음(단순 DOM
  위치 이동).

## 확인한 것

- `npm test` — 14개 파일, 240개 테스트 전부 통과.
- `npm run build` — `dist/index.html` 단일 파일(46.6KB, gzip 15.7KB) 생성.
- Playwright로 375 / 768 / 1024px 스크린샷: 홈(1열→2열 타일), 문제 화면(세로셈 정렬,
  keypad+grid 세로→가로), 기록실(표, 불꽃 아이콘, 잠긴 카피바라 자리) 육안 확인.
- 스타일시트에 `:root` 밖 원시 hex 없음(`grep` 로 확인), `url(http...)`/`@import`/CDN
  없음(`grep` 로 확인).

## 후속 수정 (같은 날, 코디네이터 검수 반영)

Chrome 에서 768×1024 로 직접 재본 결과, 숫자판이 `.quiz-board`(flex) 안에서
shrink-to-fit 되어 버튼이 40×52px 까지 눌린 문제가 지적됐다 — `#pad`/`.keypad` 가
`width:auto`(또는 `max-width`만 있는) 상태로 flex item 안에 있으면, 브라우저가
grid의 내재 크기(max-content)로 좁혀버려 의도한 240px 에 닿지 못했다. 375px 에서도
같은 문제가 있었다(세로 배치라도 `align-items:center` 이면 폭은 fit-content 로 계산됨).

- `.keypad` 에 `width:240px`(고정, `max-width:100%` 로 극단적으로 좁은 경우만 방어)과
  `flex:0 0 auto` 를 줘서 shrink 되지 않게 했다.
- `.keypad button` 을 `height:52px` 고정 대신 `aspect-ratio:1` + `min-width/min-height:48px` 로
  바꿔, 커진 폭에 맞춰 키가 정사각형으로 자라게 했다(가늘고 긴 대신 통통한 정사각형이
  손끝에 더 잘 잡힌다).
- `.quiz-board #pad` 에 `flex:0 0 auto` 를 (768px 미디어 쿼리 안이 아니라) 항상 적용되게
  옮겼다.

측정 결과:

| 뷰포트 | 배치 | 버튼 크기 |
|---|---|---|
| 375px | 세로(스택) | 74.66 × 74.66px |
| 768px | 가로(나란히) | 74.66 × 74.66px |

44px 최소 기준을 요구했던 명세보다도 넉넉하고, 요청받은 48px 최소치도 여유 있게 넘는다.

**320px 확인** (가장 좁은 실사용 폰, 세 자리 × 한 자리에서 실제 올림수 칸(`p0-k1`)이
나오는 문제로 확인): `document.documentElement.scrollWidth - clientWidth === 0`
(가로 스크롤 없음), `p0-k1`(올림수)과 `p0-c1`(그 올림이 들어가는 곱셈 칸)의 중심 x좌표가
정확히 160.0px 로 소수점까지 일치 — 정렬이 픽셀 단위로 그대로 유지된다. 세로셈 관련
CSS(`.g-*`, `.g-row*`)는 이번 수정에서 한 줄도 건드리지 않았다(diff로 확인) — 정렬은
애초에 깨질 수 없는 변경이었다.

`npm test` 240개 재확인, `npm run build` 재확인.

## 두 번째 후속 수정 (같은 날) — 세로셈이 숫자판보다 작아 보이던 문제

768×1024 에서 재보니 격자(140×318, 칸 44px)가 숫자판(240×323, 키 75px)보다 작고
연했다 — 아이 눈이 가야 할 세로셈이 입력 도구보다 작은 "각주"처럼 보인다는 지적이었다.
칸 크기가 뷰포트와 무관하게 44px 로 고정돼 있던 게 원인이었다.

- `:root` 에 `--cell-size: clamp(44px, calc(44px + 20 * (100vw - 320px) / 448), 64px)` 를
  추가했다 — 320px 에서 정확히 44px(바닥), 768px 이상에서 정확히 64px(그 이상도 64px 로
  고정)이 되도록 계수를 역산했고, 그 사이는 뷰포트에 비례해 매끄럽게 커진다.
- `.g-cell` 의 너비/높이/글자 크기를 전부 `--cell-size` 기반 비율식으로 바꿨다
  (글자 크기는 원래 24/44 비율(6/11)을 그대로 유지하는 `calc(var(--cell-size) * 6 / 11)`).
- 올림수 칸(`.g-carry`)은 새 변수 `--carry-size: calc(var(--cell-size) * 30 / 44)` 로 원래
  비율(30/44, 글자 15/44)을 그대로 유지하며 같이 커지게 했다. 좌우 여백은
  `(--cell-size - --carry-size) / 2` 로 대수적으로 계산해서, 올림수 칸이 차지하는 총
  너비가 정확히 칸 하나와 같다는 불변식(원래 코드가 기대던 정렬 메커니즘)을 뷰포트 폭과
  무관하게 그대로 지켰다. 올림수 줄 높이(`--cell-size * 32/44`)와 구구단 답칸 폭
  (`--cell-size * 45/22`, 원래 90/44 비율)도 같은 방식으로 스케일했다.
- 숫자판은 반대로 **64px 로 고정 상한**을 뒀다: `.keypad` 에 `--key-size: 64px` 를 두고
  `grid-template-columns: repeat(3, var(--key-size))` 로 칸을 고정 px 로 지정했다 —
  이건 이전 40px 버그(1fr + flex 조상의 shrink-to-fit)의 근본 원인 자체를 없앤다: 트랙이
  고정 px 이면 grid 의 max-content 폭이 항상 `3 * 64px + gap` 으로 결정되어, 더 이상
  flex 조상 안에서 애매하게 줄어들 여지가 없다.

### 측정치 (Playwright, 실제 rebuild 된 dist/index.html)

| 뷰포트 | 배치 | 격자 칸 | 올림수 칸 | 숫자판 키 | 격자 박스 | 숫자판 박스 |
|---|---|---|---|---|---|---|
| 320px | 세로 스택 | 44 × 44px | 30 × 30px | 64 × 64px | — | — |
| 375px | 세로 스택 | 46.45 × 46.45px | 31.67 × 31.67px | 64 × 64px | — | — |
| 768px | 가로 나란히 | 64 × 64px | 43.6 × 43.6px | 64 × 64px | 268 × 452.5px | 208 × 288px |

768px에서 격자(268px 폭)가 숫자판(208px 폭)보다 뚜렷하게 커져 목표(세로셈이 주인공, 숫자판은
옆의 도구)를 만족한다.

### 정렬 재검증 (세 뷰포트 모두, 실제 올림수가 있는 문제로)

| 뷰포트 | 올림수 칸 | 그 올림이 들어가는 곱셈 칸 | 중심 x 차이 |
|---|---|---|---|
| 320px | `p0-k1` | `p0-c1` | 0px (정확히 일치) |
| 375px | `p0-k2` | `p0-c2` | 0px (정확히 일치) |
| 768px | `p0-k1` | `p0-c1` | 0px (정확히 일치) |

세 폭 모두 가로 스크롤 없음(`scrollWidth − clientWidth === 0`). 768px 예시(47×36 류
문제)에서 두 번째 부분곱의 1의 자리 칸(`p1-c0`)이 DOM에 아예 없음을 확인해 그 칸이
여전히 비어 있음(박스조차 없음)을 재확인했다. 빈 올림수 줄 collapse 로직
(`isEmptyCarryRow`)은 `verticalGrid.js` 를 전혀 건드리지 않았으므로 그대로다.

`npm test` 240개 재확인, `npm run build` 재확인.

## 세 번째 후속 수정 (같은 날) — 기본 버튼 색과 결과 화면 버튼 배치

### 1. 「한 판 더」 버튼이 칙칙해진 문제

앞서 흰 글자 대 밝은 `--accent` 배경이 2.39:1로 실패하는 걸 고치면서 배경을
`--accent-text`(#8a4e10, 어두운 갈색)로 바꿨는데, 그 결과 앱에서 가장 신나야 할
"한 판 더" 버튼이 칙칙한 갈색이 됐다는 지적을 받았다. 대비를 지키면서도 밝은
주황을 잃지 않는 다른 조합이 있었다 — 배경은 그대로 밝은 `--accent`
(`#f0912f`) 로 두고, 글자를 `--ink`(`#33220f`)로 어둡게 하는 쪽.

직접 계산 스크립트로 검증: **`--ink` on `--accent` = 6.39:1** (AA 4.5:1을 여유
있게 통과). `--accent-text` 를 배경으로 쓰는 곳이 이 버튼 하나뿐이었음을
`grep` 으로 확인했고(다른 곳은 전부 `--accent-text` 를 글자색으로만 쓰고
있었다 — `.result-xp`, `.result-badges`, `.result-series`, `.quiz-message`,
`.tile em` 등, 그대로 둬도 맞는 용도), `.btn-primary` 만 배경을 `--accent`,
글자를 `--ink` 로 바꿨다. 결과: 버튼은 원래의 명랑한 주황으로 돌아왔고 대비도
통과한다.

### 2. 결과 화면 버튼이 768px에서 양쪽으로 갈라지던 문제

측정해보니 `.result-buttons`(`justify-content: center`)는 멀쩡했는데,
`.btn-primary` 에 걸린 `margin: 16px auto 0`(3-값 shorthand라 좌우가 모두
`auto`)이 원인이었다. flex 컨테이너 안에서 auto 여백은 justify-content 보다
우선해서 여유 공간을 흡수하는데, "한 판 더" 하나에만 좌우 auto 여백이 걸려
있으니 그 여백들이 여유 공간을 나눠 가지면서 "한 판 더"를 따로 떨어뜨려
놓고 나머지 두 버튼(틀린 문제 다시보기/집으로)을 오른쪽 끝으로 밀어버렸다.
768px 에서 실측: `again`(한 판 더) [200.2–314.6], `review` 시작 474.8 —
사이에 160px 의 빈 틈. 이 auto 여백은 원래 `.btn-primary` 가 혼자 쓰일 때
(풀이 화면의 "다음 문제" 버튼처럼) 스스로 가운데 정렬하려고 넣어둔 것이라,
`.result-buttons .btn-primary { margin: 0; }` 로 이 컨테이너 안에서만 꺼서
컨테이너의 `justify-content: center` 가 세 버튼을 정상적으로 한 무리로
묶게 했다. `.btn-primary` 가 쓰이는 다른 곳(`explain.js`)은 여전히 flex
컨테이너 밖에서 혼자 쓰이므로 그대로 auto 여백으로 가운데 정렬된다.

기록실 화면도 같은 문제가 있는지 768/1024에서 확인했는데, 기록실의 버튼들은
전부 `.btn-primary` 가 아니라 `.btn-ghost` 라 auto 여백이 없어 문제가 없었다
— 고칠 게 없었다.

### 3. 결과 화면 내용이 1024px에서 왼쪽에 쏠려 보이던 문제

`#app` 자체는 1024px에서도(760px, 가운데 정렬) 멀쩡했다 — 문제는 결과 화면의
내용(카피바라 한 마리 + 짧은 문구 + 버튼 세 개)이 그 762px 폭을 다 채울
필요가 없는데도 왼쪽 정렬된 채로 놓여, 오른쪽에 넓은 빈 공간이 남아 "왼쪽에
쏠린" 것처럼 보였던 것이다. `.result` 에 `max-width: 480px; margin: 0 auto;`
를 줘서 스스로 읽기 좋은 폭으로 제한하고 가운데 정렬되게 했다. 세로셈
화면(quiz-board)의 폭 요구사항 때문에 필요한 `#app` 의 넓은 max-width(760px)는
전혀 건드리지 않았다.

### 측정 결과 (Playwright, rebuild 된 dist/index.html)

| 항목 | 값 |
|---|---|
| `.btn-primary` 배경 | `rgb(240,145,47)` (`--accent`) |
| `.btn-primary` 글자 | `rgb(51,34,15)` (`--ink`) |
| `--ink` on `--accent` 대비 | **6.39:1** (AA 통과) |
| 768px, 버튼 간 간격 | again[200.2–314.6] → gap 8px → review[322.6–480.25] → gap 8px → home[488.25–567.78] — 세 버튼이 하나의 무리 |
| 768px, `.result` 폭/위치 | 480px, [144–624] (768 안에서 정확히 가운데) |
| 1024px, `.result` | 스크린샷으로 확인 — 읽기 좋은 폭의 카드로 가운데 정렬, 더 이상 왼쪽에 쏠려 보이지 않음 |
| 기록실 화면 768/1024 | 버튼이 전부 `.btn-ghost` 라 애초에 문제 없음 — 수정 없음 |

세로셈/숫자판 관련 CSS(`.g-*`, `.keypad*`, `.quiz-board*`, `--cell-size`,
`--key-size`)는 이번 수정에서 전혀 건드리지 않았다.

`npm test` 240개 재확인, `npm run build` 재확인.
